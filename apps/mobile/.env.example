# apps/mobile environment (Expo). Copy to .env.
# Only EXPO_PUBLIC_* are exposed to the app bundle (anon key is RLS-bound).

EXPO_PUBLIC_SUPABASE_URL="https://[ref].supabase.co"
EXPO_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"
EXPO_PUBLIC_APP_URL="https://hediyola.com"
